import * as S from './styles';

import imageLoading from '../../assets/loading.gif';

export function Loading() {
  return <S.Loading src={imageLoading} alt="Loading com Pokebolas" title="Loading com Pokebolas" />;
}
